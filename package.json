{
  "name": "@ryohey/wavelet",
  "version": "0.0.1",
  "description": "A wavetable synthesizer that never stops the UI thread created by AudioWorklet.",
  "main": "index.js",
  "scripts": {
    "start": "npx http-server public -c-1 --cors",
    "watch": "tsc --watch"
  },
  "author": "ryohey",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.4.2"
  }
}
